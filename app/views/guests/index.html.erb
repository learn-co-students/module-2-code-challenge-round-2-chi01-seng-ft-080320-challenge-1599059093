<h1>All The Guests</h1>
<%= button_to "View all episodes", episodes_path, method: :get %>
<%= button_to "Make an Appearance", new_appearance_path, method: :get %>

<ul>
  <% @guests.each do |guest| %>
    <li><%= link_to(guest.name, guest_path(guest)) %></li>
  <% end %>
</ul>
